---
phase: 05-educational-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/agents/research_orchestrator/orchestrator.py
  - src/utils/carousel_renderer.py
  - src/utils/carousel_service.py
autonomous: true
requirements:
  - EDU-02
  - EDU-03

must_haves:
  truths:
    - "ResearchOrchestrator has a run_educational() public method that returns exactly 1 Story with lesson steps in key_stats"
    - "Educational lesson steps in key_stats use the format 'STEP N: VERB\\nExplanation sentence'"
    - "image_query in educational Story targets tool visuals, not news thumbnails"
    - "carousel_renderer.render_carousel() accepts a content_type param; when 'educational', Slide 2 is WHAT YOU'LL LEARN instead of hook stat"
    - "The new _slide_learn_preview() builder renders accent-bg WHAT YOU'LL LEARN header + 3-4 step bullets in white text"
    - "carousel_service.create_carousel() accepts content_type and passes it through to render_carousel()"
  artifacts:
    - path: "src/agents/research_orchestrator/orchestrator.py"
      provides: "run_educational() method + _synthesise_educational() private method"
      contains: "run_educational"
    - path: "src/utils/carousel_renderer.py"
      provides: "content_type param on render_carousel() + _slide_learn_preview() builder"
      contains: "_slide_learn_preview"
    - path: "src/utils/carousel_service.py"
      provides: "content_type param on create_carousel() passed through to render_carousel()"
      contains: "content_type"
  key_links:
    - from: "src/agents/research_orchestrator/orchestrator.py"
      to: "src/utils/story.py"
      via: "run_educational returns Story with content_type='educational' set"
      pattern: "content_type.*educational"
    - from: "src/utils/carousel_service.py"
      to: "src/utils/carousel_renderer.py"
      via: "create_carousel passes content_type= to render_carousel() call"
      pattern: "content_type=content_type"
---

<objective>
Add the educational synthesis method to ResearchOrchestrator, the educational Slide 2 builder to the carousel renderer, and thread content_type through carousel_service so render_carousel() receives it. These are coordinated changes spanning three files that all depend on Plan 01's content_type field.

Purpose: Without an educational synthesis prompt, `run_educational()` doesn't exist and the pipeline branch added in Plan 01 raises AttributeError. Without the carousel renderer change, educational carousels render a blank/broken Slide 2 (empty hook_stat_value). Without the carousel_service pass-through, content_type is silently dropped at the service layer and render_carousel() always defaults to news mode, breaking EDU-03.

Output: run_educational() on ResearchOrchestrator (calls _synthesise_educational with a different Claude prompt), _slide_learn_preview() builder in the renderer with render_carousel() routing on content_type, and create_carousel() in carousel_service updated to accept and thread content_type.
</objective>

<execution_context>
@/Users/hareenedla/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hareenedla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-educational-pipeline/05-RESEARCH.md
@.planning/phases/05-educational-pipeline/05-01-SUMMARY.md

@src/agents/research_orchestrator/orchestrator.py
@src/utils/carousel_renderer.py
@src/utils/carousel_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add run_educational() and _synthesise_educational() to ResearchOrchestrator</name>
  <files>src/agents/research_orchestrator/orchestrator.py</files>
  <action>
    Read the existing `orchestrator.py` first to understand the `run()` method and `_synthesise()` private method structure before making changes.

    Add two new methods to the `ResearchOrchestrator` class:

    **`async def run_educational(self, topic: str) -> list[Story]`** — public entry point for educational content:
    - Runs the same Exa + Tavily + Serper parallel research as `run()` (reuse the existing `asyncio.gather` research step)
    - Then calls `_synthesise_educational(topic, results)` instead of `_synthesise(topic, results)`
    - Returns a list containing exactly 1 Story (wrap in list if needed)
    - Sets `story.content_type = "educational"` on the returned Story
    - Sets `story.source = "exa"` (or the dominant source) — keep consistent with existing pattern

    **`async def _synthesise_educational(self, topic: str, results: dict) -> Story`** — private synthesis with educational-specific prompt:
    - Uses the same `anthropic.AsyncAnthropic` client and `claude-haiku-4-5-20251001` model as the existing `_synthesise()` method
    - Prompt key differences from the news synthesis prompt:
      1. Returns exactly 1 Story (not 5 angles)
      2. `key_stats` must contain 5-7 lesson steps formatted as `"STEP N: VERB\nExplanation sentence."` — example: `"STEP 1: OPEN A NEW CONVERSATION\nStart with a clear task description and give Claude the role it should play."`
      3. `hook_stat_value` and `hook_stat_label` must be empty strings `""` — educational carousels don't use the hook stat slide
      4. `headline` must be the topic reframed as a direct instruction: e.g. "How to use Claude for work" → "HOW TO USE CLAUDE FOR WORK (AND ACTUALLY GET RESULTS)"
      5. `image_query` must target tool visuals, not news thumbnails: e.g. for "how to use Claude for work" → `"Claude AI interface screenshot"`. Instruct Claude to generate a visual-asset query (logo, interface, screenshot) not a news article image query.
      6. `summary` is a 1-2 sentence intro that frames the guide value: "Here's the step-by-step process most people skip when they first try [topic]."
    - The LLM response must be parsed into a `Story` object the same way `_synthesise()` does — use the same JSON parsing pattern
    - Prompt the LLM to return JSON with fields: `headline`, `summary`, `key_stats` (list of 5-7 steps), `hook_stat_value` (always ""), `hook_stat_label` (always ""), `image_query`
    - If LLM returns malformed JSON, raise `ResearchError("Educational synthesis failed: {reason}")` — same error pattern as existing method
  </action>
  <verify>
    <automated>cd /Users/hareenedla/Hareen/insta && python -c "from src.agents.research_orchestrator.orchestrator import ResearchOrchestrator; o = ResearchOrchestrator(); assert hasattr(o, 'run_educational'), 'run_educational missing'; assert hasattr(o, '_synthesise_educational'), '_synthesise_educational missing'; print('ResearchOrchestrator methods OK')"</automated>
    <manual>Review the _synthesise_educational prompt: key_stats should show "STEP N: VERB\nExplanation" format example in the prompt text, not just in docstring. image_query instructions must explicitly say "logo, screenshot, or interface visual — NOT a news article image".</manual>
  </verify>
  <done>ResearchOrchestrator.run_educational() exists, sets story.content_type='educational', returns list of exactly 1 Story. _synthesise_educational() uses a Claude prompt that produces 5-7 lesson steps in key_stats with the "STEP N: VERB\nExplanation" format and an empty hook_stat_value.</done>
</task>

<task type="auto">
  <name>Task 2: Add educational Slide 2 builder to carousel renderer + thread content_type through carousel_service</name>
  <files>
    src/utils/carousel_renderer.py
    src/utils/carousel_service.py
  </files>
  <action>
    Read both carousel_renderer.py and carousel_service.py before editing.

    **carousel_renderer.py — add _slide_learn_preview() and update render_carousel():**

    Add `_slide_learn_preview()` function:

    **`def _slide_learn_preview(steps_preview: list[str], total: int) -> Image.Image`:**
    - Returns a 1080x1350px Pillow image (W x H, same as all other slides)
    - Background: ACCENT color (`#8075FF` / RGB 128,117,255) — the same periwinkle used for accent highlights, NOT the dark navy BG. This makes Slide 2 visually distinct as the preview/hook slide.
    - Call `_draw_brand(draw)` and `_draw_slide_counter(draw, 2, total)` — same as other slide builders
    - Title text: "WHAT YOU'LL LEARN" in Anton font, white, large (same size as hook stat label, approx 52-58px). Position in upper third.
    - Divider line: thin horizontal rule in white at ~40% opacity below the title (same pattern as other slides if they have dividers; or a simple 2px white line)
    - Steps preview: render the first 3-4 items from `steps_preview` as a bullet list in Inter font, white text, ~36px. Extract only the first line of each step (before the `\n` if present) to keep it concise — e.g. from `"STEP 1: OPEN A NEW CONVERSATION\nStart with..."` show `"• STEP 1: OPEN A NEW CONVERSATION"`. Leave ~60px top padding between bullets.
    - If `steps_preview` has fewer than 3 items, render what's available — no crash.

    **Update `render_carousel()` signature and body:**
    - Add `content_type: str = "news"` parameter (after `source_url` in the existing signature — check the exact current signature before editing)
    - In the body, at the point where Slide 2 is built (currently calls `_slide_hook_stat(hook_stat_value, hook_stat_label, total)`), add a conditional:
      ```python
      if content_type == "educational":
          slides.append(_slide_learn_preview(stats[:4], total))
      else:
          slides.append(_slide_hook_stat(hook_stat_value, hook_stat_label, total))
      ```
    - No other changes to `render_carousel()` — content slides, bookmark slide, CTA slide logic are unchanged.

    **carousel_service.py — thread content_type through to render_carousel():**

    The existing `create_carousel()` function (line 102) calls `render_carousel()` at line 133 without a `content_type` argument. This means educational content always renders news-style Slide 2 — broken.

    Make two definitive changes to `create_carousel()`:
    1. Add `content_type: str = "news"` as a new parameter to `create_carousel()` (add after `source_url` in the signature).
    2. In the `render_carousel(...)` call body, add `content_type=content_type` as a keyword argument.

    This is a definitive required change — not conditional. The default value `"news"` ensures all existing callers (PostCreatorAgent and any other callers that don't pass content_type) continue to work without modification.
  </action>
  <verify>
    <automated>cd /Users/hareenedla/Hareen/insta && python -c "from src.utils.carousel_renderer import render_carousel, _slide_learn_preview; import inspect; sig = inspect.signature(render_carousel); assert 'content_type' in sig.parameters; print('render_carousel content_type param OK'); slide = _slide_learn_preview(['STEP 1: TEST\nExplanation', 'STEP 2: ANOTHER\nMore text'], 8); print(f'_slide_learn_preview returned image: {slide.size}')"</automated>
    <automated>cd /Users/hareenedla/Hareen/insta && python -c "import inspect; from src.utils.carousel_service import create_carousel; sig = inspect.signature(create_carousel); assert 'content_type' in sig.parameters; print('create_carousel content_type param OK')"</automated>
    <manual>Visually confirm: when content_type='educational', Slide 2 has accent bg (#8075FF), "WHAT YOU'LL LEARN" header, and step bullets. When content_type='news', Slide 2 still shows the hook stat (no regression).</manual>
  </verify>
  <done>_slide_learn_preview() returns a 1080x1350 Pillow image with accent bg, WHAT YOU'LL LEARN title, and 3-4 step bullets. render_carousel() routes to the correct Slide 2 builder based on content_type. create_carousel() accepts content_type and passes it to render_carousel(). News carousel Slide 2 behaviour is unchanged.</done>
</task>

</tasks>

<verification>
- `python -c "from src.agents.research_orchestrator.orchestrator import ResearchOrchestrator; from src.utils.carousel_renderer import render_carousel, _slide_learn_preview; from src.utils.carousel_service import create_carousel; print('All imports OK')"` — no errors
- ResearchOrchestrator has both `run_educational` and `_synthesise_educational` methods
- `_synthesise_educational` prompt instructs LLM to return 5-7 steps formatted as "STEP N: VERB\nExplanation" with empty hook_stat_value
- `render_carousel()` has `content_type` param; routes Slide 2 correctly
- `create_carousel()` has `content_type` param; passes it to `render_carousel()`
- `_slide_learn_preview()` produces valid Pillow image (no crash with empty list or 1-item list)
- Existing news carousel unaffected — both functions default to `content_type="news"`
</verification>

<success_criteria>
- ResearchOrchestrator.run_educational(topic) returns list of 1 Story with content_type='educational'
- Story.key_stats from educational synthesis uses "STEP N: VERB\nExplanation" format
- Story.hook_stat_value and hook_stat_label are both empty strings for educational
- render_carousel(content_type='educational') produces WHAT YOU'LL LEARN as Slide 2
- render_carousel(content_type='news') produces hook stat as Slide 2 (no regression)
- create_carousel(content_type='educational') passes content_type through to render_carousel()
</success_criteria>

<output>
After completion, create `.planning/phases/05-educational-pipeline/05-02-SUMMARY.md`
</output>
